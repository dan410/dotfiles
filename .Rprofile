options(repos = c(CRAN = "https://cran.rstudio.org"))



# define shortcut to common functions in interactive mode
h <- utils::head



if (interactive()) {
  .First <- function(){
   # cowsay::say("Mooo", by = 'cow')
  }
}

